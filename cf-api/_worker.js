const ORIGIN="https://irratiamendiak.github.io";
const SERVICE="IRRATIGIS_BACKEND";
function cors(origin=""){return {"Access-Control-Allow-Origin":origin===ORIGIN?origin:ORIGIN,"Access-Control-Allow-Methods":"GET, POST, OPTIONS","Access-Control-Allow-Headers":"Content-Type, Authorization, security-token, security-user-id","Vary":"Origin"}}
export default {async fetch(request,env){const origin=request.headers.get("Origin")||"";if(request.method==="OPTIONS")return new Response(null,{status:204,headers:cors(origin)});const u=new URL(request.url);if(!u.pathname.startsWith("/api/"))return new Response("Not found",{status:404,headers:cors(origin)});const upstream=await env[SERVICE].fetch(new Request(request));const outHeaders=new Headers(upstream.headers);outHeaders.set("Cache-Control","no-store");for(const [k,v] of Object.entries(cors(origin)))outHeaders.set(k,v);return new Response(upstream.body,{status:upstream.status,statusText:upstream.statusText,headers:outHeaders})}};
